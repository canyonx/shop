{% extends 'base.html.twig' %}

{% block title %}
	{{ category.name }}
{% endblock %}

{% block body %}
	<h1 class="title">{{ category.name }}</h1>
	<div class="row">
		{% for p in products %}

			<div class="col-3">
				{% include "shared/_card.html.twig" %}
			</div>

		{% endfor %}
	</div>
{% endblock %}
